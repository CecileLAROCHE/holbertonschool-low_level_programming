#include <stdio.h>

/**
* main - Entry point
*
* ecrit ("Programming is like building a multilingual puzzle) et reviens
* à la ligne automatiquement.
*
* Return: Always 0 (Success)
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
/*puts = ecrit en rajoutant une ligne vide a la fin*/
return (0);
}

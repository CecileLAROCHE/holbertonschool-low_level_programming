#include <stdio.h>

/**
* main - Entry point
*
* Description: Longer description of the function
*
* Return: 0
*/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

if (n<0) {
printf(n is negative);
}
else if(n=0) {
printf(n is zero) ;
}
else if(condition3) {
printf(n is negative)
}
return (0);
}

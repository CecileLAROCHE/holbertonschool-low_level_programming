#include "main.h"

/**
* _abs - Entry point
* @i: The character to be checked.
*
*Écrivez une fonction qui calcule la valeur absolue d'un entier.
*
*Prototype : int _abs(int);
*
* Return: 0
*/

int _abs(int i)
{
if (i > 0)
	{
	return (i);
	}
else
{
	return (-i);
}
}
